export interface Project {
  title: string;
  description: string;
  imgUrl: string;
  url?: string;
  category?: string;
}

export interface Skill {
  title: string;
  level: string;
  img: string;
}
